// Geometry is deliberately separate from the compiled texture contract.
// A small coding model should implement one bounded task from LOW-COST-TASKS.md at a time.
export function buildProp({ box, fixedFitting, adaptiveField }, contract, scale = [1, 1, 1]) {
  const parts = [];
  // parts.push(box(...));
  // parts.push(fixedFitting(...));
  // parts.push(adaptiveField(...));
  return parts;
}
