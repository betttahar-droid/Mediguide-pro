# Copy to .env and fill in. .env is gitignored and must stay that way.
#
# Used only by tools/authoring/ — the concept-sheet generator is a desktop
# authoring step (§11), never a build, CI or runtime dependency. The game does
# not read this and does not need it.
GEMINI_API_KEY=
